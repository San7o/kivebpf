resources:
- manager.yaml

# Enabled self-signed certificate creation in an init container for webhooks
patches:
- path: manager_webhook_patch.yaml
- path: manager_webhook_init_patch.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: kivebpf
  newName: localhost:5001/kivebpf
  newTag: latest
